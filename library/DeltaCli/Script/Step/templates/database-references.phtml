<?php
foreach ($references as $foreignKeyColumn => $reference) {
    printf(
        '"%s":%s -> "%s":%s [ id=%d ];',
        $tableName,
        $foreignKeyColumn,
        $reference['table'],
        $reference['column'],
        $this->edgeId
    );

    $this->edgeId += 1;
}
?>